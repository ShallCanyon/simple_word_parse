
for
char ch = 'a';
#include <stdio>

int main()
{
	int i = 0;
	int time = 10;
	// the single quote is detected halfly when whitespace not dealt well
	char ch = 'a';  
	while(i < time)
	{
		printf(ch++); /* print 10 characters */
		if(i     ==      4)
			printf("\nThis is blank\n");
        else
            printf("\n");
		i = i + 1;
	}
	return 0;
}
